language: bash
warnings_are_errors: true
sudo: required

env:
 global:
   - CRAN: http://cran.rstudio.com

before_install:
  - sudo apt-get update
  - sudo apt-get install -y gcc r-base r-base-dev

script:
  - bash test
