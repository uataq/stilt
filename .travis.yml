language: r
cache: packages

dist: trusty
sudo: false

warnings_are_errors: true

branches:
  except:
    gh-pages

addons:
  apt:
    sources:
      - r-packages-trusty
    packages:
      - bash gcc libhdf5-serial-dev libnetcdf-dev netcdf-bin r-base r-base-dev r-recommended unzip 

script:
  - bash .test
